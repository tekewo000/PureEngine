# AGENTS.md

## 作業範囲と完了条件

- 実装依頼は、必要な変更・下記の検証・影響する文書の更新まで進める。初回実装や提案だけで止めず、変更に起因する失敗を修正する。レビュー・調査のみの依頼ではコードを変更しない。
- 依頼内のローカル編集・検証・修正は都度の承認を求めず進める。既存の実装から判断できる細部は任せられたものとし、結果を左右する未確定の仕様だけ確認する。回答待ちでも独立した作業は進める。
- 調査は変更対象と影響範囲に絞り、既存の実装や標準機能を優先する。依頼外の整理や将来用の抽象化を追加せず、ユーザーの作業中の差分を保持する。
- 完了報告は変更内容・検証結果・残る問題を簡潔に示す。進められない場合は、具体的な原因と必要な入力を示し、未完了を明記する。

## 言語

- ソースコード内に書く文章はすべて英語にする。日本語は新たに追加せず、既存の日本語を見つけたら英語に直す。
- 対象はソースコードのコメント・XMLドキュメントコメント・例外／ログ／UIメッセージ・テスト名や説明を含む。
- `README.md` や `docs/` などのMarkdown文書・コミットメッセージ・PR文は対象外とし、日本語でよい。
- 固有名詞の引用など日本語が必要な場合を除き、ソースコード内に日本語を残さない。

## C# の書き方

- このリポジトリは `global.json` で .NET SDK 11 RC1 固定、`net11.0` / C# 15 既定が前提。
- 常にそのSDKで使える最新のC#の書き方を使う。古い等価な書き方は選ばない。
  - 例: `ToArray()` よりコレクション式 `[.. xs]`、`new Dictionary<,>(comparer)` より `[with(comparer)]`。
- 新しい構文に確信がない場合は、Roslyn の提案・Microsoft Learn の最新ドキュメントを確認し、下記の品質チェックに含まれるビルドで検証する。
- `PureEngine.Analyzers` のみ、外部エディターのRoslynでも読み込めるよう `netstandard2.0` を対象にする。ビルドSDKとC# 15は他のプロジェクトと同じ。

## 診断・自動修正・検証

- 診断ルールの正本は `.editorconfig`。Zedの青線に相当する提案レベルも解消し、通常ビルドの警告ゼロだけで完了にしない。
- ユーザーが明示的に依頼しない限り、namespaceの名前・有無・宣言形式を一切変更しない。namespace関連の診断除外も維持する。
- C#や解析設定を変更したら、リポジトリルートで `./tools/code-quality.ps1 -Check` を実行する。提案レベルの解析・警告をエラー扱いにしたビルド・Core/Editorチェックがすべて通ることを確認する。文書のみの変更では不要。
- 追加の検証は変更した振る舞いや回帰リスクに合わせる。必須チェックが通った後は、追加変更・失敗・未解決の懸念がある場合だけ検証を広げるか再実行する。
- 一括修正には `./tools/code-quality.ps1` を使い、適用後の差分を確認する。修正対象外や自動修正できない診断は手動で対応する。
- static化・未使用引数の削除・引数順序の変更は、全呼び出し元とリフレクション・画面バインディングへの影響を確認してから行う。`[Start]`・`[Update]`・`[Destroy]` はインスタンスメソッドを維持し、必要だが使わない引数は削除せず `_` と命名する。
- 診断を消すためだけにルールを無効化・弱化しない。仕様上必要な例外は、対象の宣言や最小範囲だけに理由付きで抑制する。異常系テストの意図や後片付けの安全確認を壊さない。
- ゲームのライフサイクル属性付きメソッドのIDE0051は `PureEngine.Analyzers` のSuppressorで扱う。生成するゲーム用csprojへAnalyzer参照を自動追加し、通常の未使用メソッドには診断を残す。IDE0051全体の無効化やゲームコードへの個別pragma追加で代用しない。
- GitHub Actionsも同じ `-Check` を実行する。検証が失敗・未実行なら、完了報告にその理由を記載する。

## ドキュメントの参照・更新

- 操作・反映手順には `README.md`、設計と適用範囲には `docs/EngineArchitecture.md`、実装状況・制限・検証の証跡には `docs/ImplementationPlan.md` を必要に応じて参照する。全件の通読は求めず、変更の影響がある文書だけ更新する。
- 実装済み・ローカル検証済み・CIや実画面で確認済みを区別する。
- 診断対策では、エンジンの品質設定と生成ゲーム用csprojのAnalyzer参照を区別して説明する。規約や実装の詳細を複数の文書に重複させず、正本へのリンクを使う。

<!-- 運用方針の参考（2026-09-23確認）: https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra -->
