# AGENTS.md

## C# の書き方

- このリポジトリは `global.json` で .NET SDK 11 RC1 固定、`net11.0` / C# 15 既定が前提。
- 常にそのSDKで使える最新のC#の書き方を使う。古い等価な書き方は選ばない。
  - 例: `ToArray()` よりコレクション式 `[.. xs]`、`new Dictionary<,>(comparer)` より `[with(comparer)]`。
- 新しい構文に確信がない場合は、Roslyn の提案・Microsoft Learn の最新ドキュメントを確認してから書き、必ずビルドで検証する。
- `PureEngine.Analyzers` のみ、外部エディターのRoslynでも読み込めるよう `netstandard2.0` を対象にする。ビルドSDKとC# 15は他のプロジェクトと同じ。

## 診断・自動修正・検証

- 診断ルールの正本は `.editorconfig`。Zedの青線に相当する提案レベルも解消し、通常ビルドの警告ゼロだけで完了にしない。
- ユーザーが明示的に依頼しない限り、namespaceの名前・有無・宣言形式を一切変更しない。namespace関連の診断除外も維持する。
- C#や解析設定を変更したら、リポジトリルートで `./tools/code-quality.ps1 -Check` を実行する。提案レベルの解析・警告をエラー扱いにしたビルド・Core/Editorチェックがすべて通ることを確認する。文書のみの変更では不要。
- 一括修正には `./tools/code-quality.ps1` を使い、適用後の差分を確認する。修正対象外や自動修正できない診断は手動で対応する。
- static化・未使用引数の削除・引数順序の変更は、全呼び出し元とリフレクション・画面バインディングへの影響を確認してから行う。`[Start]`・`[Update]`・`[Destroy]` はインスタンスメソッドを維持し、必要だが使わない引数は削除せず `_` と命名する。
- 診断を消すためだけにルールを無効化・弱化しない。仕様上必要な例外は、対象の宣言や最小範囲だけに理由付きで抑制する。異常系テストの意図や後片付けの安全確認を壊さない。
- ゲームのライフサイクル属性付きメソッドのIDE0051は `PureEngine.Analyzers` のSuppressorで扱う。生成するゲーム用csprojへAnalyzer参照を自動追加し、通常の未使用メソッドには診断を残す。IDE0051全体の無効化やゲームコードへの個別pragma追加で代用しない。
- GitHub Actionsも同じ `-Check` を実行する。検証が失敗・未実行なら、完了報告にその理由を記載する。
